{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docker-build",
            "type": "shell",
            "command": "docker build -t airflow-test .",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "docker-compose-up",
            "type": "shell",
            "command": "docker compose up -d",
            "group": "test"
        }
    ]
}